[
    {
        "description": "not",
        "schema": {
            "not": {"type": "integer"}
        },
        "definition": {
            "type":["object", "number", "string", "boolean", "array", "null"]
        }
    },
    {
        "description": "not multiple types",
        "schema": {
            "not": {"type": ["integer", "boolean"]}
        },
        "definition": {
            "type":["object", "number", "string", "array", "null"]
        }
    },
    {
        "description": "not more complex schema",
        "schema": {
            "not": {
                "type": "object",
                "properties": {
                    "foo": {
                        "type": "string"
                    }
                }
             }
        },
        "definition": {
            "type":["object", "number", "string", "boolean", "integer", "array", "null"],
            "required": "foo",
            "properties": {
                "foo": {
                    "type":["object", "number", "string", "boolean", "array", "null"]
                }
            }
        }
    },
    {
        "description": "forbidden property. What about additional properties, they may contain foo which it must not?",
        "schema": {
            "properties": {
                "foo": { 
                    "not": {}
                }
            }
        },
        "definition": {
            "type":["object", "number", "string", "boolean", "integer", "array", "null"]
        }
    },
    {
        "description": "not with boolean schema true",
        "schema": {"not": true},
        "definition": {}
    },
    {
        "description": "not with boolean schema false",
        "schema": {"not": false},
        "definition": {
            "type":["object", "number", "string", "boolean", "integer", "array", "null"]
        }
    }
]
